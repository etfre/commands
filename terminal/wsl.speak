is_active() => 'evan@'
list files = ls {enter}
list all = 'ls -a' {enter}
climb [<number>] = 'cd ..' + '/..' * (int($1 || 1) - 1) {enter}
drop [<number>] = extensions.run('terminal.wsl', term) extensions.call(term, drop, $1)